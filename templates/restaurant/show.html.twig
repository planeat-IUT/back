{% extends 'base.html.twig' %}

{% block title %}Restaurant{% endblock %}

{% block body %}
    <h1>Restaurant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ restaurant.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ restaurant.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ restaurant.description }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ restaurant.adresse }}</td>
            </tr>
            <tr>
                <th>Complement</th>
                <td>{{ restaurant.complement }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ restaurant.codePostal }}</td>
            </tr>
            <tr>
                <th>Siret</th>
                <td>{{ restaurant.siret }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ restaurant.longitude }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ restaurant.latitude }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ restaurant.mail }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ restaurant.telephone }}</td>
            </tr>
            <tr>
                <th>Rib</th>
                <td>{{ restaurant.rib }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ restaurant.logo }}</td>
            </tr>
            <tr>
                <th>Nb_table</th>
                <td>{{ restaurant.nbTable }}</td>
            </tr>
            <tr>
                <th>A_decouvrir</th>
                <td>{{ restaurant.aDecouvrir ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_restaurant_index') }}">back to list</a>

    <a href="{{ path('app_restaurant_edit', {'id': restaurant.id}) }}">edit</a>

    {{ include('restaurant/_delete_form.html.twig') }}
{% endblock %}
